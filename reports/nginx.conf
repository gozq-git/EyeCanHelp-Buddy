server {
    listen       80;
    server_name  localhost;

    root   /usr/share/nginx/html;

    # Landing page lists every report; sub-folders are browsable directly.
    index  index.html;

    location / {
        autoindex on;            # directory listing for the raw report folders
        try_files $uri $uri/ =404;
    }
}
