# Test & QA dependencies (not installed in the production image).
# Install with: pip install -r requirements.txt -r requirements-dev.txt
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0
pytest-html==4.1.1
aiosqlite==0.20.0      # in-memory SQLAlchemy engine for integration tests
locust==2.32.5         # stress / load testing

# Coordinator agent runtime deps. Not needed by the backend service itself, but
# agents/coordinator/{agent,main}.py import them at module scope, so the unit
# tests cannot exercise those modules without them. Keep in sync with
# agents/coordinator/requirements.txt.
langgraph>=0.2.0,<0.4.0
# Pinned deliberately. Left unpinned, pip backtracks to bedrock-agentcore 0.0.1
# (which predates the top-level `BedrockAgentCoreApp` export) because every later
# release needs uvicorn>=0.34.2, and 0.1.1+ additionally needs boto3>=1.39.7
# against the boto3==1.39.0 pin. 0.1.0 is the newest release that works with that
# pin, and is what the coordinator image itself resolves.
bedrock-agentcore==0.1.0
